import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';import {resolve} from 'node:path';
import {extractYoutubeId,slugifyStudyTitle,isValidStudySlug,bibleReferenceLabel,validateBibleReference} from '../src/domain/study.ts';
import {StaticStudyRepository} from '../src/repositories/StaticStudyRepository.ts';import {resolveBibleLivrePreview} from '../src/services/biblePreview.ts';
const fixture=(overrides={})=>({id:'fixture-1',slug:'estudo-de-teste',title:'Estudo de teste',publishedAt:'2026-01-02',summary:'Fixture isolada, nunca publicada.',youtubeUrl:'https://www.youtube.com/watch?v=dQw4w9WgXcQ',youtubeId:'',transcript:'## Título\nTexto seguro.',references:[{book:'romanos',chapter:8,verseStart:28,verseEnd:30}],status:'published',...overrides});
assert.equal(extractYoutubeId('https://www.youtube.com/watch?v=dQw4w9WgXcQ'),'dQw4w9WgXcQ');assert.equal(extractYoutubeId('https://youtu.be/dQw4w9WgXcQ?t=3'),'dQw4w9WgXcQ');assert.equal(extractYoutubeId('https://youtube.com/shorts/dQw4w9WgXcQ'),'dQw4w9WgXcQ');assert.equal(extractYoutubeId('https://example.com/watch?v=dQw4w9WgXcQ'),null);assert.equal(extractYoutubeId('inválida'),null);
assert.equal(slugifyStudyTitle('A Graça e a Vida Cristã'),'a-graca-e-a-vida-crista');assert(isValidStudySlug('a-graca-e-a-vida-crista'));assert(!isValidStudySlug('Slug Inválido'));
const repo=new StaticStudyRepository([fixture({id:'old',slug:'antigo',publishedAt:'2025-01-01'}),fixture({id:'draft',slug:'rascunho',publishedAt:'2027-01-01',status:'draft'}),fixture({id:'new',slug:'recente',publishedAt:'2026-01-01'})]);const published=await repo.listPublished();assert.deepEqual(published.map(item=>item.slug),['recente','antigo']);assert.equal((await repo.findLatestPublished())?.slug,'recente');assert.equal(await repo.findPublishedBySlug('rascunho'),null);
assert(validateBibleReference({book:'romanos',chapter:8,verseStart:28}));assert(validateBibleReference({book:'romanos',chapter:8,verseStart:28,verseEnd:30}));assert(!validateBibleReference({book:'romanos',chapter:8,verseStart:30,verseEnd:28}));assert.equal(bibleReferenceLabel({book:'romanos',chapter:8,verseStart:28}),'Romanos 8:28');assert.equal(bibleReferenceLabel({book:'romanos',chapter:8,verseStart:28,verseEnd:30}),'Romanos 8:28–30');
const preview=resolveBibleLivrePreview({version:'blivre',book:'ROM',chapter:8,verses:[{number:28,text:'Texto 28'},{number:29,text:'Texto 29'},{number:30,text:'Texto 30'}]},{book:'romanos',chapter:8,verseStart:28,verseEnd:30});assert.equal(preview.version,'Bíblia Livre');assert.equal(preview.verses.length,3);
const root=resolve(import.meta.dirname,'..');const source=await readFile(resolve(root,'src/data/studies.ts'),'utf8');assert.match(source,/staticStudies: Study\[\] = \[\]/,'fonte pública deve iniciar vazia');const home=await readFile(resolve(root,'src/pages/index.astro'),'utf8');assert.doesNotMatch(home,/mock-study|Uma fé que atravessa|Esperança em tempos/);assert.match(home,/studyRepository\.listPublished/);const modal=await readFile(resolve(root,'public/scripts/study-reference-preview.js'),'utf8');assert.match(modal,/addEventListener\('cancel'/);assert.match(modal,/trigger\?\.focus/);assert.match(modal,/bible-data\/blivre/);assert.match(modal,/showModal/);const page=await readFile(resolve(root,'src/pages/estudos/index.astro'),'utf8');assert.match(page,/Os estudos bíblicos serão publicados aqui em breve/);
const bibleReader=await readFile(resolve(root,'public/scripts/bible-reader.js'),'utf8');assert.match(bibleReader,/id="v\$\{verse\.number\}"/,'cada versículo precisa expor âncora navegável');assert.match(bibleReader,/location\.hash\.match\(\/\^#v/,'a Bíblia precisa posicionar a referência recebida');
console.log('Testes de Estudos: aprovados.');
