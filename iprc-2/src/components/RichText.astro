---
import type { RichTextDocument } from '../domain/bulletin';
import { renderRichTextHtml } from '../services/richTextPresentation';
interface Props { document: RichTextDocument; }
const { document } = Astro.props;
---
<div class="rich-text" set:html={renderRichTextHtml(document)}></div>
