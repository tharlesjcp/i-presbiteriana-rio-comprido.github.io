export {};
type Bulletin=Record<string,any>;
const format=(value:string)=>new Intl.DateTimeFormat('pt-BR',{timeZone:'UTC',day:'2-digit',month:'long',year:'numeric'}).format(new Date(`${value}T00:00:00Z`));
fetch('/api/public/bulletins').then(async response=>{if(!response.ok)throw new Error();const {data}=await response.json() as {data:Bulletin[]},bulletin=data[0];if(!bulletin)return;const card=document.querySelector('[data-home-bulletin]') as HTMLElement,copy=document.querySelector('[data-home-bulletin-copy]') as HTMLElement,eyebrow=document.createElement('p'),meta=document.createElement('p'),title=document.createElement('h3'),link=document.createElement('a'),arrow=document.createElement('span');eyebrow.className='eyebrow';eyebrow.textContent='Boletim atual';meta.className='meta';meta.textContent=`Nº ${bulletin.number} · ${format(bulletin.date)}`;title.textContent=bulletin.pastoral.title;link.className='button button-light';link.href=`/boletins/${bulletin.slug}`;link.appendChild(document.createTextNode('Ler boletim '));arrow.setAttribute('aria-hidden','true');arrow.textContent='→';link.appendChild(arrow);copy.replaceChildren(eyebrow,meta,title,link);card.classList.remove('bulletin-card-empty');}).catch(()=>{});
